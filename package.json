{
  "name": "daily-dev-connector",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Hosted MCP connector for the daily.dev Public API on Cloudflare Workers — token stays a Worker secret; usable as a claude.ai custom connector, in Claude Code, and in scheduled cloud runs.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "node --test test/connector.test.mjs test/memory.test.mjs",
    "verify": "node harness/verify-connector.mjs"
  },
  "dependencies": {
    "@modelcontextprotocol/server": "^2.0.0",
    "zod": "^4.0.0"
  },
  "devDependencies": {
    "@modelcontextprotocol/client": "^2.0.0",
    "wrangler": "^4.0.0"
  },
  "engines": { "node": ">=20" }
}
